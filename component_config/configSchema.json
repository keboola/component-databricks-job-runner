{
  "title": "Configuration Schema",
  "type": "object",
  "required": [
    "base_url",
    "job_id"
  ],
  "properties": {
    "auth_type": {
      "propertyOrder": 5,
      "title": "Authentication method",
      "type": "string",
      "enum": [
        "token",
        "service_principal"
      ],
      "options": {
        "enum_titles": [
          "Personal Access Token",
          "Service Principal (OAuth M2M)"
        ]
      },
      "default": "token",
      "description": "How the component authenticates against Databricks."
    },
    "#api_token": {
      "propertyOrder": 10,
      "title": "Databricks API token",
      "type": "string",
      "description": "Personal Access Token used to authenticate against the Databricks API.",
      "format": "password",
      "options": {
        "dependencies": {
          "auth_type": "token"
        }
      }
    },
    "client_id": {
      "propertyOrder": 12,
      "title": "Client ID",
      "type": "string",
      "description": "Application (client) ID of the Databricks service principal.",
      "options": {
        "dependencies": {
          "auth_type": "service_principal"
        }
      }
    },
    "#client_secret": {
      "propertyOrder": 14,
      "title": "Client secret",
      "type": "string",
      "format": "password",
      "description": "OAuth secret generated for the Databricks service principal.",
      "options": {
        "dependencies": {
          "auth_type": "service_principal"
        }
      }
    },
    "base_url": {
      "propertyOrder": 20,
      "title": "Base URL",
      "type": "string",
      "default": "https://adb-2153812530704740.0.azuredatabricks.net",
      "description": "Base URL of the Databricks API instance."
    },
    "job_id": {
      "propertyOrder": 30,
      "title": "Job ID",
      "type": "integer",
      "description": "ID of the Databricks job to trigger.",
      "format": "select",
      "enum": [],
      "options": {
        "async": {
          "label": "Load jobs",
          "action": "list_jobs"
        }
      }
    },
    "ssl_verify": {
      "propertyOrder": 40,
      "title": "SSL verify",
      "type": "boolean",
      "default": true,
      "description": "If false, SSL verification will be turned off and untrusted certificates may be used.",
      "format": "checkbox"
    }
  }
}
